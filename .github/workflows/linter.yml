name: build
on:
  push:
    branches:
      - main
  #pull_request:
jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: install python3
        run: sudo apt install python3-pip
        
      - name: install pip install black, isort, flake8
        run: |
          pip install black
          pip install isort
          pip install flake8

#       - name: install pip install isort
#         run: pip install isort
        
#       - name: install pip install 
#         run: pip install isort
        
      - name: black, isort,  chek files with flag
        run: |
          isort . --diff
          flake8 --extend-ignore E203,W234 .
          
     # black --diff .      
          
#       - name: isort check files
#         run: |
#           isort . --diff
          
          
          
        
          
        
      
        
